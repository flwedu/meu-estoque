import { Dashboard } from "@/components/dashboard";

/**
 * Página principal do sistema que exibe o dashboard
 * @returns {JSX.Element} Página principal com o dashboard
 */
export default function Home() {
	return <Dashboard />;
}
